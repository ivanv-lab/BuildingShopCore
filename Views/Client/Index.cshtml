@model IPagedList<BuildingShopCore.Models.Client>
@using BuildingShopCore.Controllers;
@using X.PagedList;
@using X.PagedList.Extensions;
@using X.Web.PagedList

<link href="~/css/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewData["Title"] = "Клиенты";
}

<div class="loader"></div>

<h2>Клиенты</h2>

<p>
    <a class="my-custom-link" asp-action="Create">Добавить клиента</a>
    <button class="my-custom-link" onclick="window.print();">Печать</button>
</p>

<form id="NewTableId" asp-action="ClientPartialView" asp-controller="Client" method="post">
    <div class="form-group">
        <p>
            Поиск: <input type="text" class="form-control" value="@ViewData["CurrentFilter"]" id="searchInput" name="SearchString" oninput="performSearch()" />
        </p>
    </div>
</form>

<div id="divClients">
    <partial name="_ClientPartialView" />
</div>

@section Scripts {
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        function performSearch() {
            var searchString = $('#searchInput').val();

            $.ajax({
                url: '@Url.Action("ClientPartialView", "Client")',
                type: 'GET',
                data: { searchString: searchString },
                success: function (data) {
                    console.log(searchString);
                    $('#divClients').html(data);
                }
            });
        }
    </script>
}
