@model IPagedList<BuildingShopCore.Models.Order>
@using BuildingShopCore.Controllers;
@using X.PagedList;
@using X.PagedList.Extensions;
@using X.Web.PagedList

<link href="~/css/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewData["Title"] = "Заказы";
}

<div class="loader"></div>

<h2>Заказы</h2>

<p>
    <a class="my-custom-link" asp-action="Create">Добавить заказ</a>
    <button class="my-custom-link" onclick="window.print();">Печать</button>
</p>

<form id="NewTableId" asp-action="OrderPartialView" asp-controller="Order" method="post">
    <div class="form-group">
        <p>
            Поиск: <input type="text" class="form-control" value="@ViewData["CurrentFilter"]" id="searchInput" name="SearchString" oninput="performSearch()" />
        </p>
    </div>
</form>

<div id="divOrders">
    <partial name="_OrderPartialView" />
</div>

@section Scripts {
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        function performSearch() {
            var searchString = $('#searchInput').val();

            $.ajax({
                url: '@Url.Action("OrderPartialView", "Order")',
                type: 'GET',
                data: { searchString: searchString },
                success: function (data) {
                    console.log(searchString);
                    $('#divOrders').html(data);
                }
            });
        }
    </script>
}
